'use strict';

import Vue from 'vue';
import Router from 'vue-router';

Vue.use(Router);

export default new Router({
    // Set mode to 'hash' if your server does not support rewriting:
    mode: 'history',

    // Add your routes here:
    routes: [
    ]
});
